import type React from "react"
import type { Metadata } from "next"
import { GeistSans } from "geist/font/sans"
import { GeistMono } from "geist/font/mono"
import { Analytics } from "@vercel/analytics/next"
import { Suspense } from "react"
import "./globals.css"

export const metadata: Metadata = {
  title: "VIMAX IT SOLUTION LLC - Professional IT Services & Consulting",
  description:
    "Custom Software Development, IT Consulting, Cloud Solutions, and Network Security Services. Innovative IT Solutions for Modern Businesses.",
  generator: "v0.app",
  keywords: "IT services, software development, IT consulting, cloud solutions, network security, Pensacola FL",
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className={`font-sans ${GeistSans.variable} ${GeistMono.variable}`}>
        <Suspense fallback={null}>{children}</Suspense>
        <Analytics />
      </body>
    </html>
  )
}
